# Storage & Data Loading

- **__init__.py** — Empty package initializer.
- **chat_storage.py** — Handles JSON-based persistence for chat conversations, including save, load, list, and delete operations.
- **data_loader.py** — Qt thread that asynchronously loads and normalizes the Excel case database to avoid blocking the UI.
